import axios from 'axios';

describe('GET /api', () => {
  it('should return a message 2', async () => {
    //const res = await axios.get(`/api`);
    //expect(res.status).toBe(200);
    //expect(res.data).toEqual({ message: 'Welcome to menu-backend!' });
  });
});
